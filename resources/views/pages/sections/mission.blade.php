<section id="mission" class="mission-premium">
    <div class="mission-container">
        <p class="mission-subtitle">
            {{ $section->getTranslation('subtitle') }}
        </p>
        <div class="mission-insight">
            <span>{{ $section->getTranslation('insight_eyebrow') }}</span>
            {{ $section->getTranslation('insight_content') }}
        </div>
    </div>
</section>
